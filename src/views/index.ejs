<!DOCTYPE html>
<html lang="en">
    <%- include ('./partials/head.ejs') %>
<body>
    <h1>Ta boa pra caramba</h1>

    <!-- usando javascript dentro do ejs -->
    <!-- <% const message = " HELLO"%>
    <h1><%= message %></h1> -->

    <div class="container">
        <div class="content">
            <%  if(users.length > 0)  { %> <% users.forEach(user => {    %>
                <h3 class="title">
                    <%=  user.fistName  %> <%= user.lastName%>
                </h3>

                <p><%= user.email %></p>
                <% }) %> <% } else { %>
                    <p>Não há usuarios para exibir</p>
                    <% } %>

        </div>
    </div>


</body>
</html>